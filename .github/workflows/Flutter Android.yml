name: 🤖 Flutter Android Build and Deploy(Production)
run-name: "🚀 Android Build by ${{ github.actor }} on ${{ github.ref_name }}"

permissions:
  contents: write
  checks: write
  pull-requests: write

on:
  push:
    branches: [ main ]
    paths:
      - "android/**"
      - "lib/**"
      - "pubspec.yaml"
      - "pubspec.lock"
      - "assets/**"

  # pull_request:
  #   branches: [ main, developer ]
  #   paths:
  #     - "android/**"
  #     - "lib/**"
  #     - "pubspec.yaml"
  #     - "pubspec.lock"

jobs:
  android-ci:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: ⚙️ Setup Flutter (3.35.7 Stable)
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.35.7'
          channel: 'stable'

      - name: 📦 Install Dependencies
        run: flutter pub get

      - name: 🔐 Export Razorpay Secret
        run: |
          echo "RAZORPAY_KEY_ID=${{ secrets.RAZORPAY_KEY_ID }}" >> $GITHUB_ENV
      # ------------------------------
      # LINT (With success flag)
      # ------------------------------
      - name: 🔍 Lint Check
        id: lint
        run: |
          flutter analyze
          if [ $? -eq 0 ]; then
            echo "success=true" >> $GITHUB_OUTPUT
          else
            echo "success=false" >> $GITHUB_OUTPUT
          fi
        continue-on-error: true

      # ------------------------------
      # Install junitreport
      # ------------------------------
      - name: 📦 Install junitreport
        run: dart pub global activate junitreport

      # ------------------------------
      # TEST (JSON + Convert to JUnit)
      # ------------------------------
      - name: 🧪 Run Tests (JUnit Output)
        id: test
        run: |
          export PATH="$HOME/.pub-cache/bin:$PATH"
          flutter test --machine --no-pub > test_output.json || true
          dart pub global run junitreport:tojunit < test_output.json > test-results.xml || true
          if grep -q "<failure" test-results.xml; then
            echo "success=false" >> $GITHUB_OUTPUT
          else
            echo "success=true" >> $GITHUB_OUTPUT
          fi
        continue-on-error: true

      # ------------------------------
      # Publish Tests
      # ------------------------------
      - name: 📈 Publish Test Results
        uses: dorny/test-reporter@v1
        if: always()
        with:
          name: Flutter Test Results
          path: test-results.xml
          reporter: java-junit
          fail-on-error: false

      # ------------------------------
      # Summary
      # ------------------------------
      - name: 🧾 Add Lint + Test + Build Summary
        if: always()
        run: |
          echo "## 🤖 Flutter Android CI Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 🔍 Lint Issues" >> $GITHUB_STEP_SUMMARY
          # Run analyze without crashing CI
          flutter analyze --no-fatal-warnings --no-fatal-infos > lint.log || true
          # Show only meaningful issues
          if grep -E "error •|warning •|info •" lint.log >/dev/null; then
            grep -E "error •|warning •|info •" lint.log | head -n 20 >> $GITHUB_STEP_SUMMARY
          else
            echo "✅ No lint issues found" >> $GITHUB_STEP_SUMMARY
          fi
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 🧪 Test Summary" >> $GITHUB_STEP_SUMMARY
          if [ ! -f test-results.xml ]; then
            echo "⚠️ No test results found — tests may have crashed." >> $GITHUB_STEP_SUMMARY
            TOTAL=0
            FAILED=0
            PASSED=0
          else
            TOTAL=$(grep -o "<testcase" test-results.xml | wc -l)
            FAILED=$(grep -o "<failure" test-results.xml | wc -l)
            PASSED=$((TOTAL - FAILED))
          fi
          echo "- **Total Tests:** $TOTAL" >> $GITHUB_STEP_SUMMARY
          echo "- **Passed:** 🟢 $PASSED" >> $GITHUB_STEP_SUMMARY
          echo "- **Failed:** 🔴 $FAILED" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          if [ "$FAILED" -gt 0 ]; then
            echo "**Failed Tests (Top 10):**" >> $GITHUB_STEP_SUMMARY
            grep -oP '(?<=testcase name=").*?(?=")' test-results.xml | head -n 10 >> $GITHUB_STEP_SUMMARY
          fi
          
      # ------------------------------
      # FINAL GATE — BLOCK DEPLOY IF FAILED
      # ------------------------------
      - name: ❌ Fail CI if Lint or Tests Failed
        if: always()
        run: |
          if [ "${{ steps.lint.outputs.success }}" != "true" ] || [ "${{ steps.test.outputs.success }}" != "true" ]; then
            echo "❌ Lint or Tests Failed — stopping deploy"
            exit 1
          else
            echo "✅ Lint & Tests Passed — deploy allowed"
          fi
          
      - name: 🔐 Decode Android Keystore
        run: echo "${{ secrets.ANDROID_KEYSTORE }}" | base64 -d > android/app/android.keystore

      # Create key.properties
      - name: 🔧 Configure Signing
        run: |
          cat > android/key.properties <<EOF
          storePassword=${{ secrets.KEYSTORE_PASSWORD }}
          keyPassword=${{ secrets.KEY_PASSWORD }}
          keyAlias=${{ secrets.KEY_ALIAS }}
          storeFile=android.keystore
          EOF

      - name: 🔢 Auto version bump using run number
        run: |
          VERSION_LINE=$(grep "^version:" pubspec.yaml)
          VERSION_NAME=$(echo "$VERSION_LINE" | cut -d '+' -f 1 | sed 's/version: //')

          # Correct way
          NEW_VERSION_CODE=$GITHUB_RUN_NUMBER

          echo "🔧 versionName: $VERSION_NAME"
          echo "🔧 New versionCode: $NEW_VERSION_CODE"

          sed -i "s/^version:.*/version: $VERSION_NAME+$NEW_VERSION_CODE/" pubspec.yaml

          grep version: pubspec.yaml

      - name: 🏗️ Build Android APK & AAB
        run: |
          flutter build apk --release
          flutter build appbundle --release
      - name: 📤 Upload Build Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: flutter-build
          path: |
            build/**/app-release.aab
            build/**/app-release.apk
            build/**/release/*.apk
            build/**/release/*.aab
  android-deploy:
    name: "🚀 Android Deploy to Google Play"
    runs-on: ubuntu-latest
    needs: android-ci
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    environment:
      name: playstore-deploy   # 👈 Nishant must approve

    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 📥 Download Build Artifacts
        uses: actions/download-artifact@v4
        with:
          name: flutter-build

      - name: 🗃️ List downloaded files
        run: ls -R .

      - name: 📦 Move AAB and APK to root for Fastlane
        run: |
          find . -name "*.aab" -exec mv {} app-release.aab \;
          find . -name "*.apk" -exec mv {} app-release.apk \;
  
      - name: 🧼 Remove APK (Google Play only accepts AAB)
        run: |
          if [ -f app-release.apk ]; then
            rm app-release.apk
          fi
      - name: 🔐 Decode Android Keystore
        run: echo "${{ secrets.ANDROID_KEYSTORE }}" | base64 -d > android.keystore

      - name: 🔧 Configure Signing
        run: |
          cat > android/key.properties <<EOF
          storePassword=${{ secrets.KEYSTORE_PASSWORD }}
          keyPassword=${{ secrets.KEY_PASSWORD }}
          keyAlias=${{ secrets.KEY_ALIAS }}
          storeFile=../android.keystore
          EOF
      - name: 🔧 Setup Fastlane
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: "3.2"

      - run: gem install fastlane -N

      # - name: 🚀 Deploy to Google Play
      #   env:
      #     JSON_KEY: ${{ secrets.GOOGLE_PLAY_SERVICE_ACCOUNT_JSON }}
      #     PACKAGE_NAME: ${{ secrets.PACKAGE_NAME }}
      #   run: |
      #     echo "$JSON_KEY" > service_account.json
      #     fastlane supply \
      #       --aab app-release.aab \
      #       --json_key service_account.json \
      #       --package_name "$PACKAGE_NAME" \
      #       --track internal \
      #       --skip_upload_screenshots true \
      #       --skip_upload_images true

      - name: 🚀 Deploy to Google Play (Production)
        env:
          JSON_KEY: ${{ secrets.GOOGLE_PLAY_SERVICE_ACCOUNT_JSON }}
          PACKAGE_NAME: ${{ secrets.PACKAGE_NAME }}
        run: |
          echo "$JSON_KEY" > service_account.json
          fastlane supply \
            --aab app-release.aab \
            --json_key service_account.json \
            --package_name "$PACKAGE_NAME" \
            --track production \
            --skip_upload_metadata \
            --skip_upload_changelogs \
            --skip_upload_images \
            --skip_upload_screenshots

      # Optional — Add summary
      - name: 🧾 Build Summary
        run: |
          echo "## 🚀 Production Release Deployed" >> $GITHUB_STEP_SUMMARY
          echo "Package: ${{ secrets.PACKAGE_NAME }}" >> $GITHUB_STEP_SUMMARY
          echo "Track: **Production**" >> $GITHUB_STEP_SUMMARY

