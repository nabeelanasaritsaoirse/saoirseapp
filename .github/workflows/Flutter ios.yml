name: 🍎 iOS CI, Debug & TestFlight
run-name: "🚀 iOS Build by ${{ github.actor }} on ${{ github.ref_name }}"

on:
  push:
    branches:
      - test
    paths: ['ios/**', 'pubspec.yaml']

concurrency:
  group: ios-build-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  checks: write
  pull-requests: write

env:
  IOS_MIN_VERSION: "16.0"
  BUNDLE_ID: com.saoirse.epi
  TEAM_ID: BYQUNNQSQH
  PROFILE_NAME: "match AppStore com.saoirse.epi 1765456537"
  ARCHIVE_DIR: build/ios/archive
  IPA_DIR: build/ios/ipa

jobs:
  # ios-regenerate:
  #   name: ♻️ Regenerate iOS Platform
  #   runs-on: macos-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #       with:
  #         token: ${{ secrets.GITHUB_TOKEN }}

  #     - uses: subosito/flutter-action@v2
  #       with:
  #         channel: stable
  #         cache: true

  #     - name: 🧹 Flutter clean
  #       run: flutter clean && flutter pub get

  #     - name: 🍎 Regenerate iOS
  #       run: flutter create . --platforms=ios --org com.saoirse

  #     - name: 📦 CocoaPods setup
  #       run: |
  #         cd ios
  #         # Clean Podfile for Firebase
  #         cat > Podfile << 'EOF'
  #         platform :ios, '12.0'
  #         install! 'cocoapods', :deterministic_uuids => false
  #         target 'Runner' do
  #           use_frameworks!
  #           use_modular_headers!
  #           flutter_install_all_ios_pods File.dirname(File.realpath(__FILE__))
  #         end
  #         post_install do |installer|
  #           installer.pods_project.targets.each do |target|
  #             flutter_additional_ios_build_settings(target)
  #             target.build_configurations.each do |config|
  #               config.build_settings['IPHONEOS_DEPLOYMENT_TARGET'] = '12.0'
  #               config.build_settings['GCC_PREPROCESSOR_DEFINITIONS'] ||= ['$(inherited)']
  #               config.build_settings['GCC_PREPROCESSOR_DEFINITIONS'] << 'PERMISSION_CAMERA=1'
  #               config.build_settings['GCC_PREPROCESSOR_DEFINITIONS'] << 'PERMISSION_PHOTOS=1'
  #             end
  #           end
  #         end
  #         EOF
  #         pod install --repo-update
  #         cd ..

  #     - name: 🔑 Restore GoogleService-Info.plist
  #       run: |
  #         cat > ios/Runner/GoogleService-Info.plist <<EOF
  #         <?xml version="1.0" encoding="UTF-8"?>
  #         <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
  #         <plist version="1.0">
  #         <dict>
  #           <key>API_KEY</key>
  #           <string>${{ secrets.IOS_API_KEY }}</string>
  #           <key>BUNDLE_ID</key>
  #           <string>com.saoirse.epi</string>
  #           <key>CLIENT_ID</key>
  #           <string>1:YOUR_GCM_SENDER_ID:ios:YOUR_CLIENT_ID</string>
  #           <key>GCM_SENDER_ID</key>
  #           <string>${{ secrets.IOS_MESSAGING_SENDER_ID }}</string>
  #           <key>GOOGLE_APP_ID</key>
  #           <string>${{ secrets.IOS_APP_ID }}</string>
  #           <key>PROJECT_ID</key>
  #           <string>${{ secrets.IOS_PROJECT_ID }}</string>
  #           <key>STORAGE_BUCKET</key>
  #           <string>YOUR_PROJECT_ID.appspot.com</string>
  #           <key>IS_ADS_ENABLED</key>
  #           <false/>
  #           <key>IS_ANALYTICS_ENABLED</key>
  #           <true/>
  #           <key>IS_APPINVITE_ENABLED</key>
  #           <false/>
  #           <key>IS_GCM_ENABLED</key>
  #           <true/>
  #           <key>IS_SIGNIN_ENABLED</key>
  #           <false/>
  #           <key>GOOGLE_STATIC_FILES_HOST</key>
  #           <string>YOUR_PROJECT_ID.appspot.com</string>
  #         </dict>
  #         </plist>
  #         EOF

  #     - name: ✅ Verify regeneration
  #       run: |
  #         test -f ios/Runner.xcworkspace/xcshareddata/swiftpm/Package.resolved
  #         grep -q "FlutterAppDelegate" ios/Runner/AppDelegate.swift
  #         cd ios && pod ipc list | grep firebase

  #     - name: 📤 Commit clean iOS
  #       run: |
  #         git config user.name "github-actions[bot]"
  #         git config user.email "github-actions[bot]@users.noreply.github.com"
  #         git add ios/
  #         git commit -m "fix(ios): regenerate clean platform [skip ci]" || exit 0
  #         git push

  # ==================================================
  # 1️⃣ SIMULATOR DEBUG TEST
  # ==================================================
  ios-native-debug:
    name: 🧪 iOS Native Debug (Simulator)
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v4

      - name: 📝 Create .env
        run: |
          cat > .env <<EOF
          IOS_API_KEY=${{ secrets.IOS_API_KEY }}
          IOS_APP_ID=${{ secrets.IOS_APP_ID }}
          IOS_MESSAGING_SENDER_ID=${{ secrets.IOS_MESSAGING_SENDER_ID }}
          IOS_PROJECT_ID=${{ secrets.IOS_PROJECT_ID }}
          ANDROID_API_KEY=dummy
          ANDROID_APP_ID=dummy
          ANDROID_MESSAGING_SENDER_ID=dummy
          ANDROID_PROJECT_ID=dummy
          EOF

      - name: 📥 Download Fonts
        run: |
          mkdir -p assets/fonts
          cd assets/fonts
          curl -LO https://github.com/google/fonts/raw/main/ofl/poppins/Poppins-Regular.ttf
          curl -LO https://github.com/google/fonts/raw/main/ofl/poppins/Poppins-Medium.ttf
          curl -LO https://github.com/google/fonts/raw/main/ofl/poppins/Poppins-SemiBold.ttf
          curl -LO https://github.com/google/fonts/raw/main/ofl/poppins/Poppins-Bold.ttf
          cd ../..

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.35.7"
          channel: stable
          cache: true

      - run: flutter clean
      - run: flutter pub get

      - name: 🔥 Create GoogleService-Info.plist
        run: |
          cat > ios/Runner/GoogleService-Info.plist <<'EOF'
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
            <key>API_KEY</key>
            <string>${{ secrets.IOS_API_KEY }}</string>
            <key>GCM_SENDER_ID</key>
            <string>${{ secrets.IOS_MESSAGING_SENDER_ID }}</string>
            <key>GOOGLE_APP_ID</key>
            <string>${{ secrets.IOS_APP_ID }}</string>
            <key>PROJECT_ID</key>
            <string>${{ secrets.IOS_PROJECT_ID }}</string>
            <key>BUNDLE_ID</key>
            <string>com.saoirse.epi</string>
            <key>STORAGE_BUCKET</key>
            <string>saoirse-epi.firebasestorage.app</string>
            <key>IS_ADS_ENABLED</key>
            <false/>
            <key>IS_ANALYTICS_ENABLED</key>
            <false/>
            <key>IS_APPINVITE_ENABLED</key>
            <true/>
            <key>IS_GCM_ENABLED</key>
            <true/>
            <key>IS_SIGNIN_ENABLED</key>
            <true/>
          </dict>
          </plist>
          EOF

      - name: 🔧 Configure iOS ${{ env.IOS_MIN_VERSION }}
        run: |
          sed -i.bak "s/platform :ios, '[0-9.]*'/platform :ios, '${{ env.IOS_MIN_VERSION }}'/" ios/Podfile
          echo "✅ Podfile updated to iOS ${{ env.IOS_MIN_VERSION }}"

      - name: ♻️ Cache CocoaPods
        uses: actions/cache@v4
        with:
          path: |
            ~/.cocoapods
            ios/Pods
          key: ${{ runner.os }}-pods-${{ hashFiles('ios/Podfile.lock') }}
          restore-keys: |
            ${{ runner.os }}-pods-

      - name: 🧹 Install Pods
        run: |
          cd ios
          rm -rf Pods Podfile.lock
          pod install --repo-update
          cd ..

      - name: 🛠️ Build for Simulator
        run: |
          flutter build ios \
            --simulator \
            --debug \
            --no-codesign

      - name: 🔍 Verify Binary Architecture
        run: |
          echo "=== Built app architecture ==="
          file build/ios/iphonesimulator/Runner.app/Runner
          lipo -info build/ios/iphonesimulator/Runner.app/Runner || true

      - name: 🚀 Boot ARM64 Simulator
        run: |
          UDID=$(xcrun simctl list devices available | grep "iPhone 16 (" | head -1 | grep -oE '\([A-Z0-9-]+\)' | tr -d '()')
          
          if [ -z "$UDID" ]; then
            echo "❌ No iPhone 16 simulator found"
            xcrun simctl list devices available | grep iPhone
            exit 1
          fi
          
          echo "✅ Booting iPhone 16: $UDID"
          xcrun simctl boot "$UDID" || echo "Already booted"
          sleep 5

      - name: 📲 Install App
        run: |
          UDID=$(xcrun simctl list devices | grep "(Booted)" | head -1 | awk -F '[()]' '{print $2}')
          
          if [ -z "$UDID" ]; then
            echo "❌ No booted simulator found"
            exit 1
          fi
          
          echo "Installing on simulator: $UDID"
          xcrun simctl uninstall "$UDID" ${{ env.BUNDLE_ID }} || true
          xcrun simctl install "$UDID" build/ios/iphonesimulator/Runner.app
          
          echo "✅ App installed successfully"

      - name: 🎬 Launch & Verify (Enhanced Logging)
        run: |
          UDID=$(xcrun simctl list devices | grep "(Booted)" | head -1 | awk -F '[()]' '{print $2}')
          
          echo "Launching app with console logging..."
          xcrun simctl launch --console "$UDID" ${{ env.BUNDLE_ID }} > launch_output.txt 2>&1 &
          LAUNCH_PID=$!
          
          # Wait and check every 2 seconds for 20 seconds
          for i in {1..10}; do
            sleep 2
            if xcrun simctl spawn "$UDID" launchctl list | grep -q "${{ env.BUNDLE_ID }}"; then
              echo "✅ App launched successfully after ${i}x2 seconds!"
              
              # Check app UI state
              echo "=== Checking app UI state ==="
              xcrun simctl spawn "$UDID" log show --predicate 'process == "Runner"' --style compact --last 10s | tail -20
              
              kill $LAUNCH_PID 2>/dev/null || true
              exit 0
            fi
            echo "Waiting for app... (attempt $i/10)"
          done
          
          echo "❌ App failed to launch within 20 seconds"
          echo "=== Launch output ==="
          cat launch_output.txt || true
          
          kill $LAUNCH_PID 2>/dev/null || true
          exit 1

      - name: 🔍 Capture Simulator Logs
        if: always()
        run: |
          UDID=$(xcrun simctl list devices | grep "(Booted)" | head -1 | awk -F '[()]' '{print $2}')
          
          echo "=== Capturing last 2 minutes of logs ===" > simulator_logs.txt
          xcrun simctl spawn "$UDID" log show --predicate 'process == "Runner"' --style compact --last 2m >> simulator_logs.txt 2>&1 || true
          
          echo "=== Logs captured ===" 
          tail -50 simulator_logs.txt

      - name: 📸 Screenshot
        if: always()
        run: |
          UDID=$(xcrun simctl list devices | grep "(Booted)" | head -1 | awk -F '[()]' '{print $2}')
          xcrun simctl io "$UDID" screenshot simulator_screenshot.png || true

      - name: 📤 Upload Debug Artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: simulator-debug-${{ github.run_number }}
          path: |
            simulator_screenshot.png
            simulator_logs.txt
            launch_output.txt

  # ==================================================
  # 2️⃣ RELEASE BUILD
  # ==================================================
  ios-ci:
    name: 🏗️ iOS CI Build
    needs: ios-native-debug
    runs-on: macos-latest
    outputs:
      version_name: ${{ steps.version.outputs.VERSION_NAME }}
      build_number: ${{ steps.version.outputs.BUILD_NUMBER }}

    steps:
      - uses: actions/checkout@v4
      
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - run: sudo gem install fastlane cocoapods xcodeproj -N
      - run: flutter clean
      - run: flutter pub get

      - name: 📝 Create .env file
        run: |
          cat > .env <<EOF
          RAZORPAY_KEY_ID=${{ secrets.RAZORPAY_KEY_ID }}
          IOS_API_KEY=${{ secrets.IOS_API_KEY }}
          IOS_APP_ID=${{ secrets.IOS_APP_ID }}
          IOS_MESSAGING_SENDER_ID=${{ secrets.IOS_MESSAGING_SENDER_ID }}
          IOS_PROJECT_ID=${{ secrets.IOS_PROJECT_ID }}
          ANDROID_API_KEY=${{ secrets.ANDROID_API_KEY }}
          ANDROID_APP_ID=${{ secrets.ANDROID_APP_ID }}
          ANDROID_MESSAGING_SENDER_ID=${{ secrets.ANDROID_MESSAGING_SENDER_ID }}
          ANDROID_PROJECT_ID=${{ secrets.ANDROID_PROJECT_ID }}
          EOF

      - name: 📥 Download Fonts
        run: |
          mkdir -p assets/fonts
          cd assets/fonts
          curl -LO https://github.com/google/fonts/raw/main/ofl/poppins/Poppins-Regular.ttf
          curl -LO https://github.com/google/fonts/raw/main/ofl/poppins/Poppins-Medium.ttf
          curl -LO https://github.com/google/fonts/raw/main/ofl/poppins/Poppins-SemiBold.ttf
          curl -LO https://github.com/google/fonts/raw/main/ofl/poppins/Poppins-Bold.ttf
          cd ../..

      - name: 🔢 Auto-Increment Build Number
        id: version
        run: |
          VERSION_LINE=$(grep "^version:" pubspec.yaml)
          VERSION_NAME=$(echo "$VERSION_LINE" | cut -d '+' -f 1 | sed 's/version: //')
          CURRENT_BUILD=$(echo "$VERSION_LINE" | cut -d '+' -f 2)
          
          # Always increment from current build number
          if [ -z "$CURRENT_BUILD" ] || [ "$CURRENT_BUILD" -lt 109 ]; then
            BUILD_NUMBER=110
          else
            BUILD_NUMBER=$((CURRENT_BUILD + 1))
          fi
          
          echo "VERSION_NAME=$VERSION_NAME" >> $GITHUB_OUTPUT
          echo "BUILD_NUMBER=$BUILD_NUMBER" >> $GITHUB_OUTPUT
          echo "🔢 Building version: $VERSION_NAME (Build $BUILD_NUMBER)"
          echo "   Previous build: $CURRENT_BUILD → New build: $BUILD_NUMBER"

      - name: 🔧 Configure iOS Settings (xcconfig only)
        run: |
          # ✅ Overwrite instead of append (deterministic)
          cat > ios/Flutter/Release.xcconfig <<'XCCONFIG'
          #include "Generated.xcconfig"
          
          STRIP_STYLE = non-global
          DEAD_CODE_STRIPPING = YES
          IPHONEOS_DEPLOYMENT_TARGET = 16.0
          ENABLE_BITCODE = NO
          ONLY_ACTIVE_ARCH = NO
          XCCONFIG
          
          echo "✅ iOS 16.0 deployment target configured via xcconfig (deterministic)"

      - name: 🔓 Setup Keychain
        run: |
          KEYCHAIN_PASSWORD="ci_keychain_pass"
          security delete-keychain build.keychain-db 2>/dev/null || true
          security create-keychain -p "$KEYCHAIN_PASSWORD" build.keychain-db
          security default-keychain -s build.keychain-db
          security unlock-keychain -p "$KEYCHAIN_PASSWORD" build.keychain-db
          security set-keychain-settings -lut 7200 build.keychain-db
          security list-keychains -d user -s build.keychain-db $(security list-keychains -d user | sed s/\"//g)

      - name: 🔐 Setup API Key
        run: |
          mkdir -p ios
          echo '${{ secrets.APP_STORE_CONNECT_API_KEY_JSON_BUILD }}' > ios/api_key.json
          chmod 600 ios/api_key.json

      - name: 🔐 Fastlane Match
        env:
          MATCH_PASSWORD: ${{ secrets.MATCH_PASSWORD }}
        run: |
          cd ios
          fastlane match appstore \
            --readonly \
            --api_key_path ./api_key.json \
            --git_url https://github.com/nabeelanasaritsaoirse/ios-certificates.git \
            --git_branch main \
            --keychain_name build.keychain-db \
            --keychain_password "ci_keychain_pass"

      - name: 🔥 GoogleService-Info.plist
        run: |
          cat > ios/Runner/GoogleService-Info.plist <<'EOF'
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
            <key>API_KEY</key>
            <string>${{ secrets.IOS_API_KEY }}</string>
            <key>GCM_SENDER_ID</key>
            <string>${{ secrets.IOS_MESSAGING_SENDER_ID }}</string>
            <key>GOOGLE_APP_ID</key>
            <string>${{ secrets.IOS_APP_ID }}</string>
            <key>PROJECT_ID</key>
            <string>${{ secrets.IOS_PROJECT_ID }}</string>
            <key>BUNDLE_ID</key>
            <string>com.saoirse.epi</string>
            <key>STORAGE_BUCKET</key>
            <string>saoirse-epi.firebasestorage.app</string>
            <key>IS_ADS_ENABLED</key>
            <false/>
            <key>IS_ANALYTICS_ENABLED</key>
            <false/>
            <key>IS_APPINVITE_ENABLED</key>
            <true/>
            <key>IS_GCM_ENABLED</key>
            <true/>
            <key>IS_SIGNIN_ENABLED</key>
            <true/>
          </dict>
          </plist>
          EOF

      - name: ✅ Validate GoogleService-Info.plist
        run: plutil -lint ios/Runner/GoogleService-Info.plist

      - name: 📦 Install Pods
        run: |
          cd ios
          pod install --repo-update

      - name: 📦 Ensure GoogleService-Info.plist is bundled (FIXED)
        run: |
          ruby <<'RUBY'
          require 'xcodeproj'

          project = Xcodeproj::Project.open('ios/Runner.xcodeproj')
          target = project.targets.find { |t| t.name == 'Runner' }
          raise 'Runner target not found' unless target

          resources = target.resources_build_phase

          # Find or create file reference in Runner group
          runner_group = project.main_group.find_subpath('Runner')
          raise 'Runner group not found' unless runner_group

          # Look for existing reference
          file_ref = runner_group.files.find { |f| f.path == 'GoogleService-Info.plist' }

          # Create if doesn't exist
          unless file_ref
            file_ref = runner_group.new_file('GoogleService-Info.plist')
            file_ref.last_known_file_type = 'text.plist.xml'
            puts '📝 Created file reference for GoogleService-Info.plist'
          end

          # Add to Copy Bundle Resources if not already there
          unless resources.files.any? { |build_file| build_file.file_ref == file_ref }
            resources.add_file_reference(file_ref)
            puts '✅ GoogleService-Info.plist added to Copy Bundle Resources'
          else
            puts 'ℹ️  GoogleService-Info.plist already in Copy Bundle Resources'
          end

          project.save
          puts '💾 Project saved'
          RUBY

          # Verify it was added
          if grep -q "GoogleService-Info.plist" ios/Runner.xcodeproj/project.pbxproj; then
            echo "✅ Verified: GoogleService-Info.plist is in project.pbxproj"
          else
            echo "❌ ERROR: Failed to add GoogleService-Info.plist to project"
            exit 1
          fi


      - name: 🔍  Check Firebase Files Before Build
        run: |
          echo "=== Checking Firebase configuration ==="
          
          if [ -f "ios/Runner/GoogleService-Info.plist" ]; then
            echo "✅ Source plist exists"
            plutil -p ios/Runner/GoogleService-Info.plist | grep -E "(PROJECT_ID|BUNDLE_ID)"
          else
            echo "❌ Source plist NOT FOUND!"
            exit 1
          fi
          
          echo "=== Checking Xcode project configuration ==="
          if grep -A 30 "PBXResourcesBuildPhase" ios/Runner.xcodeproj/project.pbxproj | grep -q GoogleService; then
            echo "✅ GoogleService-Info.plist configured in Copy Bundle Resources"
          else
            echo "❌ CRITICAL: GoogleService-Info.plist NOT in Copy Bundle Resources!"
            exit 1
          fi
          
      - name: ⚙️ Configure Xcode Signing
        run: |
          cat > update_signing.rb <<'RUBY'
          require 'xcodeproj'

          project_path = 'ios/Runner.xcodeproj'
          project = Xcodeproj::Project.open(project_path)

          project.targets.each do |target|
            if target.name == 'Runner'
              target.build_configurations.each do |config|
                if config.name == 'Release'
                  # ✅ Signing configuration only (deployment target handled by xcconfig)
                  config.build_settings['CODE_SIGN_STYLE'] = 'Manual'
                  config.build_settings['DEVELOPMENT_TEAM'] = ENV['TEAM_ID']
                  config.build_settings['CODE_SIGN_IDENTITY'] = 'Apple Distribution'
                  config.build_settings['PROVISIONING_PROFILE_SPECIFIER'] = ENV['PROFILE_NAME']
                  config.build_settings['CODE_SIGN_IDENTITY[sdk=iphoneos*]'] = 'Apple Distribution'
                  config.build_settings['ENABLE_BITCODE'] = 'NO'
                  config.build_settings['ONLY_ACTIVE_ARCH'] = 'NO'
                end
              end
            end
          end

          project.save
          RUBY

          ruby update_signing.rb
          echo "✅ Signing configured (deployment target via xcconfig only)"

      - name: 🔨 Build Flutter
        run: |
          flutter build ios \
            --release \
            --no-codesign \
            --build-name ${{ steps.version.outputs.VERSION_NAME }} \
            --build-number ${{ steps.version.outputs.BUILD_NUMBER }}

      - name: 📝 Create ExportOptions.plist
        run: |
          cat > ios/ExportOptions.plist <<EOF
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
            <key>method</key>
            <string>app-store</string>
            <key>teamID</key>
            <string>${{ env.TEAM_ID }}</string>
            <key>signingStyle</key>
            <string>manual</string>
            <key>provisioningProfiles</key>
            <dict>
              <key>${{ env.BUNDLE_ID }}</key>
              <string>${{ env.PROFILE_NAME }}</string>
            </dict>
            <key>uploadSymbols</key>
            <true/>
            <key>compileBitcode</key>
            <false/>
          </dict>
          </plist>
          EOF

      - name: 🏗️ Archive
        run: |
          cd ios
          mkdir -p ../${{ env.ARCHIVE_DIR }}
          mkdir -p ../${{ env.IPA_DIR }}

          xcodebuild archive \
            -workspace Runner.xcworkspace \
            -scheme Runner \
            -configuration Release \
            -archivePath ../${{ env.ARCHIVE_DIR }}/Runner.xcarchive \
            -destination 'generic/platform=iOS' \
            DEVELOPMENT_TEAM=${{ env.TEAM_ID }} \
            CODE_SIGN_STYLE=Manual \
            PROVISIONING_PROFILE_SPECIFIER="${{ env.PROFILE_NAME }}" \
            CODE_SIGN_IDENTITY="Apple Distribution" \
            STRIP_STYLE=non-global \
            DEAD_CODE_STRIPPING=YES \
            IPHONEOS_DEPLOYMENT_TARGET=16.0 \
            ENABLE_BITCODE=NO \
            OTHER_CODE_SIGN_FLAGS="--keychain $HOME/Library/Keychains/build.keychain-db"

      - name: 📦 Export IPA
        run: |
          cd ios
          xcodebuild -exportArchive \
            -archivePath ../${{ env.ARCHIVE_DIR }}/Runner.xcarchive \
            -exportPath ../${{ env.IPA_DIR }} \
            -exportOptionsPlist ExportOptions.plist \
            -allowProvisioningUpdates
      - name: 🔍 Inspect iOS Info.plist (CI)
        run: |
          PLIST_PATH="ios/Runner/Info.plist"
          echo "=== Raw Info.plist ==="
          plutil -p "$PLIST_PATH"

          echo "=== Launch storyboard keys ==="
          plutil -extract UILaunchStoryboardName xml1 -o - "$PLIST_PATH" || echo "No UILaunchStoryboardName"
          plutil -extract UIMainStoryboardFile xml1 -o - "$PLIST_PATH" || echo "No UIMainStoryboardFile"
          plutil -extract UIApplicationSceneManifest xml1 -o - "$PLIST_PATH" || echo "No UIApplicationSceneManifest"

        
      - name: 🔍 Check LaunchScreen.storyboard in IPA
        run: |
          IPA_PATH=$(find build/ios/ipa -name "*.ipa" | head -1)
          unzip -q "$IPA_PATH" -d /tmp/ipa_launch
          APP_PATH=$(find /tmp/ipa_launch/Payload -name "*.app" | head -1)

          echo "=== LaunchScreen.storyboard presence ==="
          find "$APP_PATH" -path "*LaunchScreen.storyboard*" || echo "❌ LaunchScreen.storyboard not found in app bundle"
          
      - name: 🔍 Show AppDelegate.swift
        run: |
          echo "=== AppDelegate.swift ==="
          cat ios/Runner/AppDelegate.swift



      - name: ✅ Verify Firebase Config in IPA
        run: |
          IPA_PATH=$(find ${{ env.IPA_DIR }} -name "*.ipa" | head -1)
          
          echo "=== Extracting IPA for Firebase validation ==="
          unzip -q "$IPA_PATH" -d /tmp/ipa_verify
          APP_PATH=$(find /tmp/ipa_verify/Payload -name "*.app" | head -1)
          
          # Check GoogleService-Info.plist exists in IPA
          PLIST_PATH="$APP_PATH/GoogleService-Info.plist"
          
          if [ ! -f "$PLIST_PATH" ]; then
            echo "❌ CRITICAL: GoogleService-Info.plist NOT found in IPA!"
            echo "   App will crash on launch with Firebase error"
            exit 1
          fi
          
          echo "✅ GoogleService-Info.plist found in IPA"
          
          # Validate plist structure
          plutil -lint "$PLIST_PATH" > /dev/null 2>&1
          if [ $? -eq 0 ]; then
            echo "✅ plist is valid XML"
          else
            echo "❌ plist is corrupted!"
            exit 1
          fi
          
          # Verify critical Firebase keys
          PROJECT_ID=$(plutil -extract PROJECT_ID raw "$PLIST_PATH" 2>/dev/null || echo "")
          BUNDLE_ID=$(plutil -extract BUNDLE_ID raw "$PLIST_PATH" 2>/dev/null || echo "")
          
          if [ -z "$PROJECT_ID" ] || [ -z "$BUNDLE_ID" ]; then
            echo "❌ Missing critical Firebase configuration!"
            exit 1
          fi
          
          echo "✅ Firebase config validated successfully:"
          echo "   PROJECT_ID: $PROJECT_ID"
          echo "   BUNDLE_ID: $BUNDLE_ID"

      - name: 📤 Upload IPA
        uses: actions/upload-artifact@v4
        with:
          name: ios-ipa
          path: ${{ env.IPA_DIR }}/*.ipa

      - name: 📤 Upload dSYM
        uses: actions/upload-artifact@v4
        with:
          name: ios-dsym
          path: ${{ env.ARCHIVE_DIR }}/Runner.xcarchive/dSYMs/

  # ==================================================
  # 3️⃣ VALIDATE IPA
  # ==================================================
  ios-validate:
    name: ✅ Validate IPA
    needs: ios-ci
    runs-on: macos-latest
    
    steps:
      - uses: actions/checkout@v4
      
      - name: 📦 Download IPA
        uses: actions/download-artifact@v4
        with:
          name: ios-ipa
          path: build/ios/ipa
      
      - name: 🔍 Comprehensive IPA Validation
        run: |
          IPA_PATH=$(find build/ios/ipa -name "*.ipa" | head -1)
          
          if [ ! -f "$IPA_PATH" ]; then
            echo "❌ IPA not found!"
            exit 1
          fi
          
          IPA_SIZE=$(du -h "$IPA_PATH" | cut -f1)
          echo "✅ IPA found: $IPA_PATH (Size: $IPA_SIZE)"
          
          # Verify IPA integrity
          unzip -t "$IPA_PATH" > /dev/null 2>&1
          if [ $? -ne 0 ]; then
            echo "❌ IPA is corrupted!"
            exit 1
          fi
          echo "✅ IPA integrity verified"
          
          # Extract IPA
          echo "=== Extracting IPA ==="
          unzip -q "$IPA_PATH" -d /tmp/ipa_extract
          APP_PATH=$(find /tmp/ipa_extract/Payload -name "*.app" | head -1)
          
          # Validate Info.plist
          echo "=== Info.plist Validation ==="
          plutil -p "$APP_PATH/Info.plist" | grep -E "(CFBundleIdentifier|CFBundleVersion|CFBundleShortVersionString|MinimumOSVersion)"
          
          # Check Firebase Configuration
          echo "=== Firebase Configuration Check ==="
          if [ -f "$APP_PATH/GoogleService-Info.plist" ]; then
            echo "✅ Firebase config found in IPA"
            plutil -p "$APP_PATH/GoogleService-Info.plist" | grep -E "(PROJECT_ID|BUNDLE_ID|GOOGLE_APP_ID)"
          else
            echo "❌ CRITICAL: Firebase config MISSING from IPA!"
            echo "   App will crash on launch!"
            exit 1
          fi
          
          # Verify Binary Architecture
          echo "=== Binary Architecture ==="
          lipo -info "$APP_PATH/Runner"
          
          # Check Critical Frameworks
          echo "=== Critical Frameworks Check ==="
          if [ -d "$APP_PATH/Frameworks" ]; then
            echo "Frameworks included:"
            ls -1 "$APP_PATH/Frameworks" | grep -E "(Firebase|Flutter)" || echo "⚠️ Firebase frameworks not found"
          fi
          
          echo ""
          echo "✅ IPA validation complete - ready for TestFlight!"

  # ==================================================
  # 4️⃣ DEPLOY TO TESTFLIGHT
  # ==================================================
  ios-deploy:
    name: 🚀 Deploy to TestFlight
    needs: [ios-ci, ios-validate]
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v4

      - name: 📦 Download IPA
        uses: actions/download-artifact@v4
        with:
          name: ios-ipa
          path: build/ios/ipa

      - name: 🚀 Install Fastlane
        run: sudo gem install fastlane -N

      - name: 🔐 Write API Key
        run: echo '${{ secrets.APP_STORE_CONNECT_API_KEY_JSON_BUILD }}' > api_key.json

      - name: 🔍 Pre-Upload Validation
        run: |
          IPA_PATH=$(find build/ios/ipa -name "*.ipa" | head -1)
          
          if [ ! -f "$IPA_PATH" ]; then
            echo "❌ IPA not found!"
            exit 1
          fi
          
          # Final integrity check
          unzip -t "$IPA_PATH" > /dev/null 2>&1
          if [ $? -eq 0 ]; then
            IPA_SIZE=$(du -h "$IPA_PATH" | cut -f1)
            echo "✅ IPA ready for upload"
            echo "   File: $(basename "$IPA_PATH")"
            echo "   Size: $IPA_SIZE"
          else
            echo "❌ IPA is corrupted!"
            exit 1
          fi

      - name: 🚀 Upload to TestFlight
        run: |
          echo "🚀 Starting TestFlight upload..."
          echo "   Version: ${{ needs.ios-ci.outputs.version_name }}"
          echo "   Build: ${{ needs.ios-ci.outputs.build_number }}"
          
          fastlane pilot upload \
            --api_key_path api_key.json \
            --ipa build/ios/ipa/*.ipa \
            --skip_waiting_for_build_processing \
            --changelog "Build ${{ needs.ios-ci.outputs.build_number }}: Automated CI/CD deployment" \
            --distribute_external false

      - name: ✅ Deployment Complete
        run: |
          echo ""
          echo "🎉 Successfully uploaded to TestFlight!"
          echo ""
          echo "📱 Version: ${{ needs.ios-ci.outputs.version_name }}"
          echo "🔢 Build: ${{ needs.ios-ci.outputs.build_number }}"
          echo ""
          echo "⏰ Next steps:"
          echo "   1. Check App Store Connect in 5-10 minutes"
          echo "   2. Build will process automatically"
          echo "   3. Add to internal testing group when ready"
          echo "   4. For external testing: Submit for Beta App Review"
          echo ""
          echo "🔗 https://appstoreconnect.apple.com"
