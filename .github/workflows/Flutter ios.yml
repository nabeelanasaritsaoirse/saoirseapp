name: 🍎 iOS CI & TestFlight
run-name: "🚀 iOS Build by ${{ github.actor }} on ${{ github.ref_name }}"

on:
  push:
    branches: [ nabeeela ]

concurrency:
  group: ios-build-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  checks: write
  pull-requests: write

env:
  BUNDLE_ID: com.saoirse.epi
  TEAM_ID: BYQUNNQSQH
  ARCHIVE_DIR: build/ios/archive
  IPA_DIR: build/ios/ipa

jobs:
  ios-ci:
    runs-on: macos-latest
    timeout-minutes: 90

    steps:
      # ---------------- Checkout ----------------
      - name: 📥 Checkout Repository
        uses: actions/checkout@v4

      # ---------------- Flutter Setup ----------------
      - name: ⚙️ Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.35.7"
          channel: stable
          cache: true

      - name: 📦 Flutter Pub Get
        run: flutter pub get

      # ---------------- Create ENV ----------------
      - name: 🔑 Create .env file
        run: |
          cat > .env <<EOF
          IOS_API_KEY=${{ secrets.IOS_API_KEY }}
          IOS_APP_ID=${{ secrets.IOS_APP_ID }}
          IOS_MESSAGING_SENDER_ID=${{ secrets.IOS_MESSAGING_SENDER_ID }}
          IOS_PROJECT_ID=${{ secrets.IOS_PROJECT_ID }}
          ANDROID_API_KEY=${{ secrets.ANDROID_API_KEY }}
          ANDROID_APP_ID=${{ secrets.ANDROID_APP_ID }}
          ANDROID_MESSAGING_SENDER_ID=${{ secrets.ANDROID_MESSAGING_SENDER_ID }}
          ANDROID_PROJECT_ID=${{ secrets.ANDROID_PROJECT_ID }}
          EOF

      - name: 📋 Copy .env to iOS Runner
        run: |
          mkdir -p ios/Runner
          cp .env ios/Runner/.env

      # ---------------- Fastlane / CocoaPods ----------------
      - name: 🚀 Install Fastlane & CocoaPods
        run: |
          sudo gem install fastlane -N
          sudo gem install cocoapods -N

      # ---------------- App Store Connect Key ----------------
      - name: 🔐 Write App Store Connect API Key
        run: |
          mkdir -p ios
          echo '${{ secrets.APP_STORE_CONNECT_API_KEY_JSON_BUILD }}' > ios/api_key.json
          chmod 600 ios/api_key.json

      # ---------------- Certificates ----------------
      - name: 🔐 Download Certificates (Match)
        env:
          MATCH_PASSWORD: ${{ secrets.MATCH_PASSWORD }}
        run: |
          set -e
          cd ios
          fastlane match appstore --readonly --api_key_path ./api_key.json --git_branch master
          cd ..

      # ---------------- Podfile Signing Fix ----------------
      - name: 📌 Export TEAM_ID for Podfile
        run: echo "TEAM_ID=${{ env.TEAM_ID }}" >> $GITHUB_ENV

      - name: 🧨 Reset Pods
        run: rm -rf ios/Podfile.lock

      - name: 🧨 Install Pods
        run: |
          cd ios
          pod install --repo-update
          cd ..

      # ---------------- Flutter build ----------------
      - name: 🔨 Build Flutter iOS (No Codesign)
        run: flutter build ios --release --no-codesign

      - name: 🔨 Prepare Flutter for Xcode 16
        run: flutter build ios --release --no-codesign --config-only

      # ---------------- Create ExportOptions.plist ----------------
      - name: 📝 Create ExportOptions.plist
        run: |
          cat > ios/ExportOptions.plist <<EOF
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN"
           "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
            <key>method</key>
            <string>app-store</string>
            <key>teamID</key>
            <string>${{ env.TEAM_ID }}</string>
            <key>signingStyle</key>
            <string>manual</string>
            <key>provisioningProfiles</key>
            <dict>
              <key>${{ env.BUNDLE_ID }}</key>
              <string>match AppStore ${{ env.BUNDLE_ID }}</string>
            </dict>
          </dict>
          </plist>
          EOF

      # ---------------- Archive ----------------
      - name: 🏗️ Archive Build (Runner only)
        run: |
          set -e
          cd ios

          mkdir -p ../${{ env.ARCHIVE_DIR }}
          mkdir -p ../${{ env.IPA_DIR }}

          xcodebuild clean archive \
            -workspace Runner.xcworkspace \
            -scheme Runner \
            -configuration Release \
            -archivePath ../${{ env.ARCHIVE_DIR }}/Runner.xcarchive \
            -destination 'generic/platform=iOS' \
            -allowProvisioningUpdates \
            | tee ../xcodebuild.log

      - name: ✅ Verify Archive
        run: |
          if [ ! -d "${{ env.ARCHIVE_DIR }}/Runner.xcarchive" ]; then
            echo "❌ Archive not created!"
            tail -n 200 xcodebuild.log || true
            exit 1
          fi
          echo "✅ Archive created successfully."
          ls -lh ${{ env.ARCHIVE_DIR }}

      # ---------------- Export IPA ----------------
      - name: 📦 Export IPA
        run: |
          set -e
          cd ios
          xcodebuild -exportArchive \
            -archivePath ../${{ env.ARCHIVE_DIR }}/Runner.xcarchive \
            -exportPath ../${{ env.IPA_DIR }} \
            -exportOptionsPlist ExportOptions.plist \
            | tee ../export.log || true

      - name: ✅ Verify IPA
        run: |
          if ls ${{ env.IPA_DIR }}/*.ipa 1>/dev/null 2>&1; then
            echo "✅ IPA Exported Successfully!"
            ls -lh ${{ env.IPA_DIR }}/*.ipa
          else
            echo "❌ IPA NOT FOUND!"
            tail -n 300 export.log || true
            exit 1
          fi

      - name: 📤 Upload IPA Artifact
        uses: actions/upload-artifact@v4
        with:
          name: ios-ipa
          path: ${{ env.IPA_DIR }}/*.ipa

      - name: 📋 Upload build logs
        uses: actions/upload-artifact@v4
        with:
          name: ios-build-logs
          path: |
            ios/xcodebuild.log
            ios/export.log


  # ---------------- TestFlight Deployment ----------------
  ios-deploy:
    name: "🚀 Deploy to TestFlight"
    needs: ios-ci
    runs-on: macos-latest
    if: github.ref == 'refs/heads/developer'

    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v4

      - name: 📦 Download IPA
        uses: actions/download-artifact@v4
        with:
          name: ios-ipa
          path: build/ios/ipa

      - name: 🚀 Install Fastlane
        run: sudo gem install fastlane -N

      - name: 🔐 Write api_key.json
        run: echo '${{ secrets.APP_STORE_CONNECT_API_KEY_JSON_BUILD }}' > api_key.json

      - name: 🚀 Upload to TestFlight
        run: |
          fastlane pilot upload \
            --api_key_path api_key.json \
            --ipa build/ios/ipa/*.ipa \
            --skip_waiting_for_build_processing
