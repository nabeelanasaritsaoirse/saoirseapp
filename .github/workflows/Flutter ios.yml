name: 🍎 iOS CI & TestFlight
run-name: "🚀 iOS Build by ${{ github.actor }} on ${{ github.ref_name }}"

on:
  push:
    tags:
      - "ios-v*"
  workflow_dispatch:

concurrency:
  group: ios-build-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  checks: write
  pull-requests: write

env:
  BUNDLE_ID: com.saoirse.epi
  TEAM_ID: BYQUNNQSQH
  PROFILE_NAME: "match AppStore com.saoirse.epi 1765456537"
  ARCHIVE_DIR: build/ios/archive
  IPA_DIR: build/ios/ipa

jobs:
  ios-ci:
    runs-on: macos-latest
    outputs:
      version_name: ${{ steps.version.outputs.VERSION_NAME }}
      build_number: ${{ steps.version.outputs.BUILD_NUMBER }}

    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: ⚙️ Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.35.7"
          channel: stable
          cache: true

      - name: 🧰 Select latest Xcode (auto)
        run: |
          echo "Available Xcodes:"
          ls /Applications | grep Xcode
      
          LATEST_XCODE=$(ls -d /Applications/Xcode*.app | sort -V | tail -n 1)
          echo "Using: $LATEST_XCODE"
      
          sudo xcode-select -s "$LATEST_XCODE/Contents/Developer"
      
          xcodebuild -version
          xcodebuild -showsdks


      - name: 📦 Flutter Pub Get
        run: flutter pub get

      - name: 🔢 Read version from pubspec & generate build number
        id: version
        run: |
          VERSION_LINE=$(grep "^version:" pubspec.yaml)
          VERSION_NAME=$(echo "$VERSION_LINE" | cut -d '+' -f 1 | sed 's/version: //')
          BUILD_NUMBER=$(echo "$VERSION_LINE" | cut -d '+' -f 2)
      
          if [ -z "$BUILD_NUMBER" ]; then
            BUILD_NUMBER=$(date +%s)
          fi
      
          echo "Version Name: $VERSION_NAME"
          echo "Build Number: $BUILD_NUMBER"
          echo "Tag: ${{ github.ref_name }}"
      
          echo "VERSION_NAME=$VERSION_NAME" >> $GITHUB_OUTPUT
          echo "BUILD_NUMBER=$BUILD_NUMBER" >> $GITHUB_OUTPUT
          echo "Resolved Version: $VERSION_NAME+$BUILD_NUMBER"


      - name: 🔑 Create .env file
        run: |
          cat > .env <<EOF
          RAZORPAY_KEY_ID=${{ secrets.RAZORPAY_KEY_ID }}
          IOS_API_KEY=${{ secrets.IOS_API_KEY }}
          IOS_APP_ID=${{ secrets.IOS_APP_ID }}
          IOS_MESSAGING_SENDER_ID=${{ secrets.IOS_MESSAGING_SENDER_ID }}
          IOS_PROJECT_ID=${{ secrets.IOS_PROJECT_ID }}
          ANDROID_API_KEY=${{ secrets.ANDROID_API_KEY }}
          ANDROID_APP_ID=${{ secrets.ANDROID_APP_ID }}
          ANDROID_MESSAGING_SENDER_ID=${{ secrets.ANDROID_MESSAGING_SENDER_ID }}
          ANDROID_PROJECT_ID=${{ secrets.ANDROID_PROJECT_ID }}
          EOF

      - name: ✅ Verify .env file
        run: |
          echo "✅ .env file created in project root"
          ls -la .env

      - name: 🚀 Install Fastlane & CocoaPods
        run: |
          sudo gem install fastlane -N
          sudo gem install cocoapods -N

      - name: 🔐 Write App Store Connect API Key
        run: |
          mkdir -p ios
          echo '${{ secrets.APP_STORE_CONNECT_API_KEY_JSON_BUILD }}' > ios/api_key.json
          chmod 600 ios/api_key.json

      - name: 🔓 Setup Keychain
        run: |
          KEYCHAIN_PASSWORD="ci_keychain_pass"
          security create-keychain -p "$KEYCHAIN_PASSWORD" build.keychain
          security default-keychain -s build.keychain
          security unlock-keychain -p "$KEYCHAIN_PASSWORD" build.keychain
          security set-keychain-settings -lut 7200 build.keychain

      - name: 🔐 Fastlane Match
        env:
          MATCH_PASSWORD: ${{ secrets.MATCH_PASSWORD }}
        run: |
          cd ios
          fastlane match appstore \
            --readonly \
            --api_key_path ./api_key.json \
            --git_branch main \
            --keychain_name build.keychain \
            --keychain_password "ci_keychain_pass"

      - name: 💾 Cache Xcode DerivedData
        uses: actions/cache@v3
        with:
          path: |
            ~/Library/Developer/Xcode/DerivedData
            ios/Pods
          key: ${{ runner.os }}-xcode-${{ hashFiles('ios/Podfile.lock') }}
          restore-keys: |
            ${{ runner.os }}-xcode-

      - name: 🧨 Reset Pods (if not cached)
        run: |
          if [ ! -d "ios/Pods" ]; then
            rm -rf ios/Podfile.lock
          fi

      - name: 📱 Configure iPhone-Only Device Family
        run: |
          /usr/libexec/PlistBuddy -c "Delete :UIDeviceFamily" ios/Runner/Info.plist || true
          /usr/libexec/PlistBuddy -c "Add :UIDeviceFamily array" ios/Runner/Info.plist
          /usr/libexec/PlistBuddy -c "Add :UIDeviceFamily:0 integer 1" ios/Runner/Info.plist
          echo "✅ Configured for iPhone-only release"

      - name: 🚫 Disable iPad & Mac Catalyst
        run: |
          # Disable Mac Catalyst
          /usr/libexec/PlistBuddy -c "Set :SupportsMacCatalyst false" ios/Runner/Info.plist || true
          /usr/libexec/PlistBuddy -c "Set :UIDeviceFamily:0 1" ios/Runner/Info.plist
      
          # Remove iPad device family if exists
          /usr/libexec/PlistBuddy -c "Delete :UIDeviceFamily:1" ios/Runner/Info.plist || true
      
          # Force only iphoneos build
          /usr/libexec/PlistBuddy -c "Add :UIRequiredDeviceCapabilities array" ios/Runner/Info.plist || true
          /usr/libexec/PlistBuddy -c "Add :UIRequiredDeviceCapabilities:0 string arm64" ios/Runner/Info.plist || true
      
          echo "✅ iPhone-only, no iPad, no Mac Catalyst"


      - name: 🔐 Verify Push Notifications Entitlement
        run: |
          echo "Checking Runner.entitlements for aps-environment..."
          if grep -q "aps-environment" ios/Runner/Runner.entitlements; then
            echo "✅ Push Notifications entitlement found"
          else
            echo "⚠️ Adding aps-environment entitlement..."
            /usr/libexec/PlistBuddy -c "Add :aps-environment string production" ios/Runner/Runner.entitlements
            echo "✅ Push Notifications entitlement added"
          fi
          cat ios/Runner/Runner.entitlements

      - name: 🧨 Fix Firebase pod mismatch & install
        run: |
          cd ios
      
          # Remove stale lock for Firebase only
          rm -rf Pods
          rm -f Podfile.lock
      
          pod repo update
          pod install

      - name: 🔨 Build Flutter iOS Release
        run: |
          echo "🚀 Starting Flutter build..."
          flutter clean
          flutter build ios --release --no-codesign
          echo "✅ Flutter build completed!"

      - name: ✅ Verify Flutter Build Output
        run: |
          echo "🔍 Checking Flutter build artifacts..."
          ls -la ios/Flutter/

          if [ ! -f "ios/Flutter/Generated.xcconfig" ]; then
            echo "❌ ERROR: Generated.xcconfig not found!"
            exit 1
          fi

          if [ ! -d "build/ios/iphoneos/Runner.app" ]; then
            echo "❌ ERROR: Runner.app not built!"
            exit 1
          fi

          echo "✅ Flutter build artifacts verified!"
          cat ios/Flutter/Generated.xcconfig

      - name: 📝 Create ExportOptions.plist
        run: |
          cat > ios/ExportOptions.plist <<EOF
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN"
           "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
            <key>method</key>
            <string>app-store</string>
            <key>teamID</key>
            <string>${TEAM_ID}</string>
            <key>signingStyle</key>
            <string>manual</string>
            <key>provisioningProfiles</key>
            <dict>
              <key>${BUNDLE_ID}</key>
              <string>${PROFILE_NAME}</string>
            </dict>
          </dict>
          </plist>
          EOF

      - name: 🏗️ Archive Build
        run: |
          cd ios
          mkdir -p ../${ARCHIVE_DIR}
          mkdir -p ../${IPA_DIR}

          xcodebuild clean archive \
            -workspace Runner.xcworkspace \
            -scheme Runner \
            -configuration Release \
            -archivePath ../${ARCHIVE_DIR}/Runner.xcarchive \
            -destination 'generic/platform=iOS,variant=iphoneos' \
            DEVELOPMENT_TEAM=${TEAM_ID} \
            CODE_SIGN_STYLE=Manual \
            PROVISIONING_PROFILE_SPECIFIER="${PROFILE_NAME}" \
            CODE_SIGN_IDENTITY="Apple Distribution" \
            OTHER_CODE_SIGN_FLAGS="--keychain build.keychain" \
            | tee ../xcodebuild.log

      - name: ✅ Verify Archive
        run: |
          if [ ! -d "${ARCHIVE_DIR}/Runner.xcarchive" ]; then
            echo "❌ Archive not created!"
            tail -n 200 xcodebuild.log || true
            exit 1
          fi

      - name: 📦 Export IPA
        run: |
          cd ios
          xcodebuild -exportArchive \
            -archivePath ../${ARCHIVE_DIR}/Runner.xcarchive \
            -exportPath ../${IPA_DIR} \
            -exportOptionsPlist ExportOptions.plist \
            | tee ../export.log || true

      - name: ✅ Verify IPA
        run: |
          ls ${IPA_DIR}/*.ipa || (echo "❌ IPA export failed!" && exit 1)

      - name: 📤 Upload IPA Artifact
        uses: actions/upload-artifact@v4
        with:
          name: ios-ipa
          path: ${{ env.IPA_DIR }}/*.ipa

      - name: 📋 Upload build logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ios-build-logs
          path: |
            xcodebuild.log
            export.log
            
      - name: 🧾 Build Summary
        run: |
          echo "## 🍎 iOS Build Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- **Version:** ${{ steps.version.outputs.VERSION_NAME }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Build Number:** ${{ steps.version.outputs.BUILD_NUMBER }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Tag:** ${{ github.ref_name }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Bundle ID:** ${BUNDLE_ID}" >> $GITHUB_STEP_SUMMARY

  ios-deploy:
    name: "🚀 Deploy to TestFlight"
    needs: ios-ci
    runs-on: macos-latest
    environment:
      name: ios-deploy   # Optional: add manual approval like Android

    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 📦 Download IPA
        uses: actions/download-artifact@v4
        with:
          name: ios-ipa
          path: build/ios/ipa

      - name: 🚀 Install Fastlane
        run: sudo gem install fastlane -N

      - name: 🔐 Write api_key.json
        run: echo '${{ secrets.APP_STORE_CONNECT_API_KEY_JSON_BUILD }}' > api_key.json

      - name: 🚀 Upload to TestFlight
        run: |
          fastlane pilot upload \
            --api_key_path api_key.json \
            --ipa build/ios/ipa/*.ipa \
            --skip_waiting_for_build_processing

      - name: 🧾 Deploy Summary
        run: |
          echo "## 🚀 TestFlight Deployment Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- **Version:** ${{ needs.ios-ci.outputs.version_name }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Build Number:** ${{ needs.ios-ci.outputs.build_number }}" >> $GITHUB_STEP_SUMMARY
          echo "- **iOS Release Tag:** ${{ github.ref_name }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Status:** ✅ Uploaded to TestFlight" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "The build will be available for testing once Apple completes processing." >> $GITHUB_STEP_SUMMARY

  ios-submit-review:
    name: 🚀 Submit to App Store Review
    needs:
      - ios-ci
      - ios-deploy
    runs-on: macos-latest
  
    environment:
      name: ios-review
  
    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4
  
      - name: 🚀 Install Fastlane
        run: sudo gem install fastlane -N
  
      - name: 🔐 Write api_key.json
        run: echo '${{ secrets.APP_STORE_CONNECT_API_KEY_JSON_BUILD }}' > api_key.json
  
      - name: 📝 Generate Release Notes
        env:
          VERSION_NAME: ${{ needs.ios-ci.outputs.version_name }}
          BUILD_NUMBER: ${{ needs.ios-ci.outputs.build_number }}
        run: |
          mkdir -p fastlane/metadata/en-US
          # Use build number to make content unique every run (forces deliver to detect a change)
          echo "Bug fixes and performance improvements. Build $BUILD_NUMBER." \
            > fastlane/metadata/en-US/release_notes.txt
          echo "✅ release_notes.txt content:"
          cat fastlane/metadata/en-US/release_notes.txt
  
      # STEP 1 — Upload metadata only, explicitly no submit
      - name: 📤 Upload Metadata (whatsNew)
        run: |
          fastlane deliver \
            --api_key_path api_key.json \
            --skip_binary_upload true \
            --skip_screenshots true \
            --submit_for_review false \
            --skip_app_version_update false \
            --force
  
      # STEP 2 — Verify whatsNew actually landed before submitting
      - name: 🔍 Verify Metadata Upload
        run: |
          echo "Metadata step completed. Pausing briefly for ASC consistency..."
          sleep 10
  
      # STEP 3 — Submit only, metadata already uploaded
      - name: 🚀 Submit Build to Apple Review
        run: |
          fastlane deliver \
            --api_key_path api_key.json \
            --skip_metadata true \
            --skip_binary_upload true \
            --skip_screenshots true \
            --submit_for_review true \
            --force
  
      - name: 🧾 Review Submission Summary
        run: |
          echo "## 🍎 App Store Submission" >> $GITHUB_STEP_SUMMARY
          echo "- Bundle: ${{ env.BUNDLE_ID }}" >> $GITHUB_STEP_SUMMARY
          echo "- Tag: ${{ github.ref_name }}" >> $GITHUB_STEP_SUMMARY
          echo "- Status: Submitted to Apple Review ✅" >> $GITHUB_STEP_SUMMARY
