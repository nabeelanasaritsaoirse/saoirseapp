name: 🍎 iOS Native Debug (Black Screen)

on:
  push:
    branches:
      - tes

jobs:
  ios-native-debug:
    runs-on: macos-15  # ✅ Changed from macos-14

    steps:
      # --------------------------------------------------
      # Checkout
      # --------------------------------------------------
      - name: 📥 Checkout repo
        uses: actions/checkout@v4

      # --------------------------------------------------
      # Flutter
      # --------------------------------------------------
      - name: ⚙️ Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.35.7"
          channel: stable
          cache: true

      # --------------------------------------------------
      # Flutter clean
      # --------------------------------------------------
      - name: 🧹 Flutter clean
        run: flutter clean

      # --------------------------------------------------
      # Pub get (Creates Generated.xcconfig)
      # --------------------------------------------------
      - name: 📦 Flutter pub get
        run: flutter pub get

      # --------------------------------------------------
      # CocoaPods (Install BEFORE building)
      # --------------------------------------------------
      - name: 📦 Install CocoaPods
        run: |
          cd ios
          rm -rf Pods Podfile.lock
          pod install --repo-update
          cd ..

      # --------------------------------------------------
      # Generate iOS config files (AFTER pods are installed)
      # --------------------------------------------------
      - name: 🛠️ Build iOS Debug
        run: flutter build ios --debug --simulator --no-codesign

      # --------------------------------------------------
      # Native Xcode build (to verify everything works)
      # --------------------------------------------------
      - name: 🔍 Native Xcode build (verbose)
        run: |
          set -o pipefail
          cd ios
          xcodebuild \
            -workspace Runner.xcworkspace \
            -scheme Runner \
            -sdk iphonesimulator \
            -configuration Debug \
            -destination 'platform=iOS Simulator,name=iPhone 16,OS=latest' \
            CODE_SIGNING_ALLOWED=NO \
            | tee xcodebuild.log

      # --------------------------------------------------
      # Upload logs
      # --------------------------------------------------
      - name: 📄 Upload native logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ios-native-debug-logs
          path: |
            ios/xcodebuild.log
